import React from "react";
import { Card, Typography } from "antd";

export default function BattleResults() {
    return (
        <Card>
            <Typography.Title level={3}>전투 결과</Typography.Title>
            <Typography.Paragraph>(준비 중) 탭만 만들어둔 상태입니다.</Typography.Paragraph>
        </Card>
    );
}